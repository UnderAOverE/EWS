# Pytest configuration: repo root on the path (imports rooted at src., matching the host app),
# tests under tests/, asyncio tests run natively.
[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
# end_pytest.ini
